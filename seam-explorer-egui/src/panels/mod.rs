//! Left/right `SidePanel` content — SEAM-01 (seam list), SEAM-02 (detail),
//! SEAM-03 (legend). Panel bodies are filled out visually by Plan 02; this
//! task wires the module structure and gets the seam-list panel far enough
//! to be real for the M1 tracer.

pub mod detail;
pub mod legend;
pub mod seam_list;
